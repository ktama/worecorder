[package]
name = "worecorder"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "1", features = ["api-all"] }

[build-dependencies]
tauri-build = { version = "1", features = ["config-json"] }
